namespace Citrina
{
    /// <summary>
    /// '1' — to return the first comment in each topic,, '2' — to return the last comment in each topic,, '0' — to return no comments. By default: '0'.
    /// </summary>
    public enum BoardTopicsPreview
    {
        First = 1,
        Last = 2,
        None = 0,
    }
}
