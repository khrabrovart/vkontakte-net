namespace Citrina
{
    /// <summary>
    /// Specifies which users are returned: '1' — to return only the current user's friends, '0' — to return all users (default).
    /// </summary>
    public enum Likes_GetList_friendsOnly
    {
        _0 = 0,
        _1 = 1,
        _2 = 2,
        _3 = 3,
    }
}
