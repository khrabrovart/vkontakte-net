namespace Citrina
{
    /// <summary>
    /// Ad format: *1 — image and text,, *2 — big image,, *3 — exclusive format,, *4 — community, square image,, *7 — special app format.
    /// </summary>
    public enum AdsUploadUrlFormat
    {
        ImageAndText = 1,
        BigImage = 2,
        ExclusiveFormat = 3,
        Community,SquareImage = 4,
        SpecialAppFormat = 7,
    }
}
