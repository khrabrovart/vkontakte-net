namespace Citrina
{
    /// <summary>
    /// '0' — do not use reverse order, '1' — use reverse order.
    /// </summary>
    public enum Market_Search_rev
    {
        Normal = 0,
        Reverse = 1,
    }
}
