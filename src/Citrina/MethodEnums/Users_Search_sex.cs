namespace Citrina
{
    /// <summary>
    /// '1' — female, '2' — male, '0' — any (default).
    /// </summary>
    public enum Users_Search_sex
    {
        Any = 0,
        Female = 1,
        Male = 2,
    }
}
