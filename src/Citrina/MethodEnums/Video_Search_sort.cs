namespace Citrina
{
    /// <summary>
    /// Sort order: '1' — by duration, '2' — by relevance, '0' — by date added.
    /// </summary>
    public enum Video_Search_sort
    {
        Duration = 1,
        Relevance = 2,
        DateAdded = 0,
    }
}
