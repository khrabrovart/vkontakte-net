namespace Citrina
{
    /// <summary>
    /// Ad format. Possible values: *'1' — image and text,, *'2' — big image,, *'3' — exclusive format,, *'4' — community, square image,, *'7' — special app format,, *'8' — special community format,, *'9' — post in community,, *'10' — app board.
    /// </summary>
    public enum AdsFormat
    {
        ImageAndText = 1,
        BigImage = 2,
        ExclusiveFormat = 3,
        CommunitySquareImage = 4,
        SpecialAppFormat = 7,
        SpecialCommunityFormat = 8,
        PostInCommunity = 9,
        AppBoard = 10,
    }
}
